link(rel='stylesheet', href='/CSS/bootstrap.min.css')
body.container
    form(action="" method='POST' enctype='application/x-www-form-urlencoded')    
        div
            table.table.table-hover
                thead
                    each user in data
                        each objet, column in user
                            td #{column}
                each user in data
                    each objet, column in user
                        td 
                            input(type="text" name=column value=objet.value)
        input(type="submit", value="Valider").input.form-control  
    p #{message}
    a(href="/home") HOME