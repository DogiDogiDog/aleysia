
link(rel='stylesheet', href='/CSS/bootstrap.min.css')
body.container
    form(action="/consultants/calendar/"+id_consultant method='POST' enctype='application/x-www-form-urlencoded')    
        div
            table.table.table-hover
                thead
                each user in data
                    each objet, column in user
                        td #{column}
                tr
                each user in data
                    each objet, column in user
                        td 
                            input(type="number" name=column value=objet.value)
        input(type="submit", value="Valider").input.form-control        
    a(href="/home") HOME
      